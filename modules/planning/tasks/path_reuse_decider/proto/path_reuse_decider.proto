syntax = "proto2";

package apollo.planning;

//////////////////////////////////
// PathReuseDeciderConfig

message PathReuseDeciderConfig {
  optional bool reuse_path = 1;
  // True to enable reuse path in lane follow
  optional bool enable_reuse_path_in_lane_follow = 2 [default = false];
}